<!-- import导入wxml  -->
<import src="/template/template.wxml" />
<!-- include引入wxml 可以将目标文件中除了<template/> <wxs/>外的所有代码引入，相当于拷贝到include的位置 -->
<include src="/template/template.wxml" />

<text>{{nowTime + '\n'}}</text>
<text>{{age > 17 ? '成年\n' : '未成年\n'}}</text>

<!-- 使用三目运算?: -->
<view class="cs-v1 {{isActive ? 'active' : ''}}"></view>
<view class="cs-v2" style="background: #82C8FF; margin-top: 10rpx;" wx:if="{{isActive}}">wx:if隐藏</view>
<button bindtap="clk_v1">点击切换颜色</button>

<!-- hidden隐藏显示   
hidden：隐藏时组件依然存在(display: none)，显示隐藏频率高时推荐使用hidden
wx:if：隐藏时组件不存在,为false时删除组件，为true时创建组件
-->
<view class="cs-v2" style="background: #82C8FF; margin-top: 10rpx;" hidden="{{isActive}}">hidden隐藏</view>

<!-- wx:if; wx:elif; wx:else;语法，条件判断，类似js中的if(){}else{} -->
<view wx:if="{{score > 89}}">优秀</view>
<view wx:elif="{{score > 79}}">良好</view>
<view wx:elif="{{score > 69}}">中</view>
<view wx:elif="{{score > 59}}">差</view>
<view wx:else>不及格</view>

<!-- block标签 
外层wx:for;wx:if...建议用block，页面加载时不会创建block,block并不是一个组件，它是一个包装元素
使用view时，页面加载时创建view，降低性能
-->
<block wx:for="{{3}}" wx:key="index">
  <button size="mini">按钮</button>
  <view>view</view>
  <text>text\n</text>
</block>

<!-- itme、index重命名
wx:key="index"或wx:key="item"解决console警告问题
 -->
<view wx:for="{{video}}" wx:key="item" wx:for-index="i">{{i + 1}} {{item}}</view>

<block wx:for="{{nummber1}}" wx:for-item="num" wx:key="index">
  <view wx:for="{{num}}" wx:key="index">
    {{item}}
  </view>
</block>

<!-- template模板复用，在不使用模板的情况下是不会进行创建的 -->
<!-- <template name="template1">
  <button size="mini">{{t1BtnText1}}</button>
  <view>{{t1ViewText1}}</view>
</template> -->
<!-- 调用模板 is="模板的name" (import已经导入) -->
<template is="template1" data="{{t1BtnText1: '按钮', t1ViewText1: '内容'}}"></template>